import { isIP } from "node:net";
import { lookup } from "node:dns/promises";
import sharp from "sharp";
const MAX_BYTES=12*1024*1024;
function privateAddress(value:string){return value==="::1"||value.startsWith("fc")||value.startsWith("fd")||value.startsWith("fe80:")||value.startsWith("127.")||value.startsWith("10.")||value.startsWith("192.168.")||/^172\.(1[6-9]|2\d|3[01])\./.test(value)||value==="0.0.0.0"}
async function safeUrl(value:string){const url=new URL(value);if(!["http:","https:"].includes(url.protocol)||url.username||url.password)throw new Error("Unsupported artwork URL");const addresses=isIP(url.hostname)?[{address:url.hostname}]:await lookup(url.hostname,{all:true});if(!addresses.length||addresses.some(item=>privateAddress(item.address)))throw new Error("Artwork host is not public");return url}
export async function remoteImage(value:string){let url=await safeUrl(value);for(let hop=0;hop<4;hop++){const response=await fetch(url,{redirect:"manual",headers:{Accept:"image/*","User-Agent":"MusicCurator/2.0"},signal:AbortSignal.timeout(15_000)});if(response.status>=300&&response.status<400){const location=response.headers.get("location");if(!location)throw new Error("Invalid artwork redirect");url=await safeUrl(new URL(location,url).toString());continue}if(!response.ok||!response.body)throw new Error(`Artwork returned ${response.status}`);const type=response.headers.get("content-type")?.split(";")[0]??"";if(!type.startsWith("image/"))throw new Error("Artwork response is not an image");const length=Number(response.headers.get("content-length")??0);if(length>MAX_BYTES)throw new Error("Artwork response is too large");const reader=response.body.getReader(),chunks:Uint8Array[]=[];let size=0;while(true){const{done,value:chunk}=await reader.read();if(done)break;size+=chunk.byteLength;if(size>MAX_BYTES){await reader.cancel();throw new Error("Artwork response is too large")}chunks.push(chunk)}const bytes=Buffer.concat(chunks.map(chunk=>Buffer.from(chunk))),metadata=await sharp(bytes).metadata();if(!metadata.width||!metadata.height||metadata.width>10000||metadata.height>10000)throw new Error("Artwork dimensions are invalid");return{bytes,type,url:url.toString()}}throw new Error("Too many artwork redirects")}
