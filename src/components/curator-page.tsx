"use client";
import { useCallback,useEffect,useState } from "react";
import { RefreshCw } from "lucide-react";
import { CuratorConsole } from "./curator-console";
type Summary={paused:boolean;metrics:Record<string,number>;operational:{running:boolean;phase:string;lastError?:string};jobs?:Array<{phase:string;status:string;subject:string;progress_json?:string}>};
export function CuratorPage(){const[data,setData]=useState<Summary|null>(null),[busy,setBusy]=useState(false),[notice,setNotice]=useState("");const refresh=useCallback(async()=>{try{const response=await fetch("/api/summary",{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error??"Curator summary unavailable");setData(body)}catch(error){setNotice(String(error))}},[]);useEffect(()=>{void refresh();const timer=setInterval(()=>void refresh(),data?.operational.running?5000:15000);return()=>clearInterval(timer)},[refresh,data?.operational.running]);async function action(name:string){setBusy(true);setNotice("");try{const response=await fetch("/api/actions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:name})});if(!response.ok)throw new Error(await response.text());setNotice(`${name.replaceAll("-"," ")} accepted.`);await refresh()}catch(error){setNotice(String(error))}finally{setBusy(false)}}return <div className="routed-workspace">{notice&&<div className="playlist-notice" role="status"><span>{notice}</span><button onClick={()=>void refresh()}><RefreshCw/>Retry</button></div>}{data?<CuratorConsole data={data} busy={busy} action={action}/>:<div className="playlist-loading"><RefreshCw className="spin"/>Loading Curator status…</div>}</div>}
