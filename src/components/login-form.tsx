"use client";
import { useState } from "react";
import { LogIn } from "lucide-react";
export function LoginForm() { const [error,setError]=useState(""),[busy,setBusy]=useState(false); async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");try{const data=new FormData(event.currentTarget),response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:String(data.get("username")),password:String(data.get("password"))})}),body=await response.json();if(!response.ok)throw new Error(body.error??"Login failed");location.href="/home"}catch(reason){setError(reason instanceof Error?reason.message:"Login failed");setBusy(false)}} return <form onSubmit={submit} className="login-form"><label><span>Navidrome username</span><input name="username" autoComplete="username" required autoFocus /></label><label><span>Navidrome password</span><input name="password" type="password" autoComplete="current-password" required /></label>{error&&<p className="login-error" role="alert">{error}</p>}<button disabled={busy} className="primary-button"><LogIn/>{busy?"Signing in…":"Enter archive"}</button></form>; }
