"use client";
import { useState } from "react";
import { LogIn } from "lucide-react";
export function LoginForm() { const [error,setError]=useState(""),[busy,setBusy]=useState(false); async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const password=String(new FormData(event.currentTarget).get("password"));const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(response.ok)location.href="/";else{setError("Login failed or temporarily rate limited.");setBusy(false)}} return <form onSubmit={submit} className="space-y-4"><label className="form-control"><span className="label mb-2">Admin password</span><input name="password" type="password" required autoFocus className="input input-bordered bg-white/70" /></label>{error&&<p className="text-error text-sm">{error}</p>}<button disabled={busy} className="btn w-full bg-[var(--teal)] text-white border-0"><LogIn size={17}/>{busy?"Checking":"Enter archive"}</button></form>; }
