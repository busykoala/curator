"use client";
import { useState } from "react";
import { LogIn } from "lucide-react";
export function LoginForm() { const [error,setError]=useState(""),[busy,setBusy]=useState(false),[legacy,setLegacy]=useState(false); async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");try{const data=new FormData(event.currentTarget),response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:legacy?undefined:String(data.get("username")),password:String(data.get("password"))})}),body=await response.json();if(!response.ok)throw new Error(body.error??"Login failed");location.href="/home"}catch(reason){setError(reason instanceof Error?reason.message:"Login failed");setBusy(false)}} return <form onSubmit={submit} className="login-form">{!legacy&&<label><span>Navidrome username</span><input name="username" autoComplete="username" required autoFocus /></label>}<label><span>{legacy?"Legacy Curator password":"Navidrome password"}</span><input name="password" type="password" autoComplete={legacy?"current-password":"current-password"} required autoFocus={legacy} /></label>{error&&<p className="login-error" role="alert">{error}</p>}<button disabled={busy} className="primary-button"><LogIn/>{busy?"Signing in…":"Enter archive"}</button><button type="button" className="legacy-login" onClick={()=>{setLegacy(value=>!value);setError("")}}>{legacy?"Use Navidrome account":"Use legacy Curator login"}</button>{legacy&&<p className="migration-note">Temporary migration access. Sign in with Navidrome afterward to enable personal playlist ownership.</p>}</form>; }
