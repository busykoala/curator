import { copyFile,mkdir,rename,rm } from "node:fs/promises";
import { dirname,relative } from "node:path";
import sharp from "sharp";
import { config } from "@/config";
import { authenticated,sameOrigin } from "@/features/auth/session";
import { db,stateSet } from "@/features/db/client";
import { getManualOverride,saveManualOverride } from "@/features/manual/overrides";
type Row={path:string;artist_name:string;album_name:string};
const text=(form:FormData,key:string):string|null=>{const value=form.get(key);return typeof value==="string"&&value.trim()?value.trim():null};
async function image(file:FormDataEntryValue|null,target:string):Promise<void>{if(!(file instanceof File)||!file.size)return;if(file.size>20*1024*1024)throw new Error("Image exceeds 20 MB");const bytes=Buffer.from(await file.arrayBuffer()),temp=`${target}.curator-manual`;await mkdir(dirname(target),{recursive:true});try{await sharp(bytes).rotate().resize({width:2000,height:2000,fit:"inside",withoutEnlargement:true}).flatten({background:"#f5f0e6"}).jpeg({quality:90,mozjpeg:true}).toFile(temp);await rename(temp,target)}finally{await rm(temp,{force:true})}}
export async function POST(request:Request){
  if(!sameOrigin(request)||!await authenticated())return new Response("Forbidden",{status:403});
  try{const form=await request.formData(),albumKey=text(form,"albumKey");if(!albumKey)return new Response("Album is required",{status:400});const rows=db().prepare("SELECT path,artist_name,album_name FROM files WHERE album_key=? ORDER BY path").all(albumKey) as Row[];if(!rows.length)return new Response("Album not found",{status:404});const first=rows[0],existing=getManualOverride(albumKey),artist=text(form,"artist")??first.artist_name,album=text(form,"album")??first.album_name,date=text(form,"date"),submittedArtistMbid=text(form,"artistMbid"),submittedReleaseGroupMbid=text(form,"releaseGroupMbid"),artistMbid=submittedArtistMbid??existing?.artist_mbid??null,releaseGroupMbid=submittedReleaseGroupMbid??existing?.release_group_mbid??null,cover=form.get("cover"),artistImage=form.get("artistImage"),hasCover=cover instanceof File&&cover.size>0,hasArtistImage=artistImage instanceof File&&artistImage.size>0,metadataChanged=artist!==first.artist_name||album!==first.album_name||Boolean(date||submittedArtistMbid||submittedReleaseGroupMbid);if(!metadataChanged&&!hasCover&&!hasArtistImage)return Response.json({error:"Enter a corrected field, a release-group ID, or verified artwork"},{status:400});
    if(metadataChanged)saveManualOverride({album_key:albumKey,artist_name:artist,album_name:album,release_date:date??existing?.release_date??null,artist_mbid:artistMbid,release_group_mbid:releaseGroupMbid});
    const albumDirs=[...new Set(rows.map(row=>dirname(row.path)))],artistDir=`${config.MUSIC_ROOT}/${relative(config.MUSIC_ROOT,first.path).split("/")[0]}`;if(hasCover){const primary=`${albumDirs[0]}/cover.jpg`;await image(cover,primary);for(const dir of albumDirs.slice(1))await copyFile(primary,`${dir}/cover.jpg`);db().prepare("INSERT OR REPLACE INTO artwork(entity_key,kind,path,provider,source_url,license,width,height) VALUES (?,'cover',?,'manual','manual-upload','personal-library',0,0)").run(albumKey,primary)}if(hasArtistImage){const target=`${artistDir}/artist.jpg`;await image(artistImage,target);db().prepare("INSERT OR REPLACE INTO artwork(entity_key,kind,path,provider,source_url,license,width,height) VALUES (?,'artist',?,'manual','manual-upload','personal-library',0,0)").run(`artist:${artist}`,target)}
    const resolved=[...(releaseGroupMbid?["identity_unresolved"]:[]),...(hasCover?["missing_album_artwork","missing_embedded_art"]:[]),...(hasArtistImage?["missing_artist_artwork"]:[])];if(resolved.length)db().prepare(`UPDATE issues SET status='resolved',updated_at=CURRENT_TIMESTAMP WHERE album_key=? AND status='open' AND code IN (${resolved.map(()=>"?").join(",")})`).run(albumKey,...resolved);
    if(metadataChanged){db().prepare("UPDATE files SET status='analyzed',updated_at=CURRENT_TIMESTAMP WHERE album_key=? AND status!='processing'").run(albumKey);stateSet("requested_action","run")}const identityPending=Boolean(db().prepare("SELECT 1 FROM issues WHERE album_key=? AND code='identity_unresolved' AND status='open' LIMIT 1").get(albumKey));return Response.json({ok:true,albumKey,identityPending})
  }catch(error){return Response.json({error:String(error)},{status:400})}
}
